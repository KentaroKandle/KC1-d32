/* state.hpp - header file for state.cpp
** This should be autogenerated from the source file, 
** but C++ is brain dead and I'm not going to spend money on 
** buying brains collected by Zombie-Farmer-Industry-LOC-complex 
** conspiracy complication */

struct SaveState {
	string fn = "snapfile.txt";
	
	int open(
		//string fn="snap0001.txt"
		);
	int close()	; 
	
	int dump();
	
	// private 
	
	int record_side();
	int record_turn();
	int record_state();
	int record_go(); // typically used, game still going.
	int record_end(); // only used if in end-state
	
};


